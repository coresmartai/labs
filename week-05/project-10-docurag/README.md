# Project 10 - DocuRAG - <your name>

<!-- This is the submission README. Fill every section, in this order: it is
     the order the reviewer reads. Delete each instruction comment as you
     complete its section. The brief (W05-P10 in the LMS) is the spec; this
     file is the evidence. -->

**Cohort:** 2026A &nbsp;&nbsp; **Branch:** `week-05-docurag` &nbsp;&nbsp; **Collection:** `docurag`

One sentence on what your assistant answers questions about, and for whom.

---

## 1. The corpus

<!-- 3 to 5 documents. Do NOT commit the PDFs; link them. Page and chunk
     counts must match your committed ingest_stats.json. -->

| # | Document title | Source URL | Pages | Chunks |
|---|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |

**Why this corpus:** two or three sentences. What made these documents worth
grounding an assistant on, and what would a real user ask them?

**Ingestion evidence:** `ingest_stats.json` (committed) is the `/stats`
output of my Week 4 KnowledgeVault after ingesting into the `docurag`
collection. Total chunks: `___`.

---

## 2. Reproduce it

<!-- The reviewer runs exactly these steps on a fresh clone. Test them on a
     fresh clone yourself before submitting; "works on my machine" is not a
     rubric line. -->

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
pytest -q                        # 6 passed, no .env needed
cp .env.example .env             # add OPENAI_API_KEY; my thresholds are already set
# Point QDRANT_LOCAL_PATH at a store holding the docurag collection
# (ingest the corpus-table documents with Week 4 KnowledgeVault, QDRANT_COLLECTION=docurag)
uvicorn app.main:app --reload    # then open http://localhost:8000
```

---

## 3. Calibration

<!-- The evidence behind your thresholds. Collect the top-1 dense score for
     every golden row (the retrieval log line prints it), then place your
     thresholds in the gap. Write the justification BEFORE your final eval
     run; a post-hoc paragraph scores half. -->

| Golden row (short) | Kind | Top-1 dense score |
|---|---|---|
|  | must-answer |  |
|  | must-answer |  |
|  | must-refuse |  |

**Chosen thresholds:** `SIMILARITY_THRESHOLD = ___` &nbsp; `SPREAD_DELTA = ___`
(set in the committed `.env.example`)

**Justification (written before the final eval run):** one paragraph. Where
do the two score distributions sit, where is the gap, why this line and not
0.05 either side. If you kept 0.55/0.08, this paragraph is where the table
has to earn that.

---

## 4. The golden set

<!-- data/golden_dataset.json: 10+ rows, 2+ refusal rows plausible for THIS
     corpus. Generated by scripts/build_golden_dataset.py, then read and
     edited by you. -->

Rows: `___` total, `___` answerable, `___` refusal.

**Edits I made after generation:** which rows you replaced or reworded, and
why. "None" is a legal answer only if you can defend every generated row in
the clinic.

**My refusal rows, and why they are plausible:** one line each. Near-misses
and adjacent topics beat random trivia.

---

## 5. The eval

<!-- results_docurag.json (committed) is the full POST /eval response over
     the committed golden set, from an unmodified eval. One paragraph of
     interpretation per metric: what the number is, WHY it is that, and what
     it hides. Reasoning is what is marked, not the size of the numbers. -->

| Metric | Value |
|---|---|
| groundedness |  |
| citation_validity |  |
| citation_recall |  |
| false_answer_rate |  |
| false_refusal_rate |  |

**Groundedness:** ...

**Citation validity:** ...

**Citation recall:** ...

**False-answer rate:** ...

**False-refusal rate:** ...

---

## 6. Three traces

<!-- One answered, one gate-refused, one marginal. Paste the JSON (or a
     screenshot of the UI trace) and give each a one-line explanation of why
     it ended where it did. The marginal one is the interesting one. -->

**Answered:** ...

**Gate-refused (`threshold_gate`):** ...

**Marginal:** ...

---

## 7. Memo

**What I would fix next, and how I would know it worked:** one paragraph.
Name the metric that would move.

**The most surprising thing in my eval:** one paragraph. Every honest eval
contains one.

---

## 8. Submission checklist

<!-- All boxes ticked before the PR leaves Draft. -->

- [ ] `pytest -q` green on a fresh clone, no `.env` present
- [ ] No PDFs, no `.env`, no API key, no `qdrant_local/`, no `.venv/` committed
- [ ] `data/golden_dataset.json` is mine (not the Attention reference set)
- [ ] `results_docurag.json`, `ingest_stats.json`, `.env.example` agree with the tables above
- [ ] Corpus table links resolve
- [ ] PR: branch `week-05-docurag`, title `Project 10 - DocuRAG`, marked Ready for review on submission day
