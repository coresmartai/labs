"""DeskOrchestrator - Week 10, Project 20 (graded).

A routed multi-agent service desk on LangGraph. One router in front of three
specialists, four memory layers behind one scoped write path, and a human
approval gate on the only calls that change the world.

This is a SCAFFOLD. Four things are yours to build; everything else runs as
shipped. See README.md, and run `pytest -q` before you write anything.
"""
__version__ = "1.0.0"
