[pytest]
# Bare `pytest` runs the offline smoke suite only. The live suite needs a
# running SUT and an OPENAI_API_KEY; run it on purpose:
#     pytest -m live tests/test_eval_harness.py -v
addopts = -m "not live"
markers =
    live: needs a running system under test and real judge API keys
testpaths = tests
